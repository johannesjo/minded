import Options from "@src/pages/options/Options";

interface SettingsWebProps {
  // Define any props that you need for this component
}

export const SettingsWeb = (props: SettingsWebProps) => {
  return <Options />;
};
