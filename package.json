{
  "private": true,
  "scripts": {
    "screenshots": "npm --prefix extension run screenshots",
    "screenshots:install": "npm --prefix extension run screenshots:install"
  },
  "version": "0.0.1"
}
